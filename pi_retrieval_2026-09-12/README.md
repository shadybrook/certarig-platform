# Frozen Phase 3 bench retrieval (12 Sep 2026)

Files copied off the Raspberry Pi that ran the frozen PoC (`certarig-phase-2-poc`). This is **World A**: port 8080 live-dashboard, not the platform sidecar on 8081.

Canonical map: [`docs/phase3-submission/README.md`](../docs/phase3-submission/README.md).

| Path | Contents |
| --- | --- |
| `phase3_evidence/` | Dated CSVs, plots, photos, READMEs — same layout as the PoC repo |
| `config/` | `rig.wave1.json` and examples as retrieved (no secrets) |
| `health/` | Pre-shutdown Pi health |
| `PI_SHA256SUMS.txt` | Checksums of the retrieval |
| `pi_bash_history.txt` | Shell history from the Pi (no passwords) |

Raw network identifiers and credentials are not in this folder.
