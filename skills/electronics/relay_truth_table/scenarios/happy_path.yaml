name: relay truth table happy path
seed: 31
procedure: relay_truth_table
duration_s: 60
events: []
expect:
  outcome: passed
  kernel_events_include: [permit_accepted, operator_safe, permit_rejected_reset_required]
  kernel_events_exclude: [pressure_high_forced_safe, estop_open_forced_safe]
  final_output_high: false
  invariants: [output_off_when_estop, output_off_when_process_unsafe, no_restart_without_reset, csv_rows_contiguous, ends_safe]
