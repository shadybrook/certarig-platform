name: thermal soak happy path
description: Jacket stays in band for a shortened soak.
seed: 21
rig: rig.thermal.sim.json
procedure: thermal_soak
label: sim-soak
duration_s: 20
events:
  - at_s: 0.1
    action: set_target
    channel: temperature
    value: 55.0
expect:
  outcome: passed
  kernel_events_exclude: [sensor_invalid_forced_safe]
  final_output_high: false
  invariants: [ends_safe]
  min_csv_rows: 20
