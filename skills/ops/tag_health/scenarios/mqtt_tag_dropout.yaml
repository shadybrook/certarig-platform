name: mqtt tag dropout
description: MQTT topic stops updating; stuck-good marks the channel invalid.
seed: 31
rig: rig.mqtt.json
capabilities: capabilities.observe.json
procedure: tag_health
label: mqtt-dropout
duration_s: 20
events:
  - at_s: 0.25
    action: fault
    kind: dropout
    channel: pressure
expect:
  outcome: failed
  kernel_events_include: [sensor_invalid_forced_safe]
  final_output_high: false
  invariants: [ends_safe]
