# CertaRig

Agentic test-operations platform: deterministic safety kernel, procedure runtime, rig simulator, provider-agnostic agent, Studio UI and evidence engine.

Quick start:

    make install
    make check

See docs/ for architecture and provenance. Full README lands with the hardware demo milestone.
